version: '3'
services:
  postgres:
    image: postgres:15
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_NAME=postgres
    ports:
      - "5432:5432"
networks:
  default:
    name: evo-wallet-dev
    external: true
